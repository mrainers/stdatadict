# stdatadict Environment For Intermediate Calculation Results

List the objects currently stored inside this environment:
`s(stdatadict::intermediates, all.names = TRUE)` Access object from this
environment: `stdatadict::intermediates$<ObjName>`

## Usage

``` r
intermediates
```

## Format

An object of class `environment` of length 0.
